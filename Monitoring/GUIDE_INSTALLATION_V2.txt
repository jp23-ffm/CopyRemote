╔═══════════════════════════════════════════════════════════════════════════════╗
║                                                                               ║
║              🚀 INSTALLATION SYSTÈME AVEC CHECKS GLOBAUX                      ║
║                                                                               ║
╚═══════════════════════════════════════════════════════════════════════════════╝


📋 RÉSUMÉ DES CHANGEMENTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

AVANT :
  • Tous les checks exécutés par chaque host toutes les minutes
  • Doublons (checks tables exécutés 2 fois)
  • Lent

MAINTENANT :
  • Checks HOST (DB, disk, workers) → toutes les minutes, par host
  • Checks GLOBAUX (tables, imports, certificat) → toutes les heures, une seule fois
  • Plus efficace, pas de doublons
  • Dashboard moderne et compact


📦 FICHIERS À INSTALLER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. MODÈLES (api/models.py)
   ✓ NodeHealthStatus (existant, ne pas toucher)
   ✓ GlobalHealthStatus (NOUVEAU - ajouter à la fin)
   
   Copie le contenu de: global_health_model.py
   
2. COMMANDES (api/management/commands/)
   ✓ update_node_health.py (REMPLACER par update_node_health_v2.py)
   ✓ update_global_health.py (NOUVEAU - créer ce fichier)

3. VUE API (api/views.py)
   ✓ ClusterStatusView (REMPLACER par cluster_view_with_global.py)

4. DASHBOARD (templates/)
   ✓ cluster_status.html (REMPLACER par cluster_status_MODERN.html)


🔧 INSTALLATION ÉTAPE PAR ÉTAPE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ÉTAPE 1: Ajouter le modèle GlobalHealthStatus
──────────────────────────────────────────────

Dans api/models.py, à la fin du fichier, AJOUTE le contenu de:
  global_health_model.py


ÉTAPE 2: Créer la migration
────────────────────────────

  python manage.py makemigrations
  python manage.py migrate


ÉTAPE 3: Mettre à jour la commande update_node_health
──────────────────────────────────────────────────────

REMPLACE api/management/commands/update_node_health.py par:
  update_node_health_v2.py

Cette nouvelle version ne fait QUE les checks spécifiques au host:
  • Database
  • Disk
  • Django processes


ÉTAPE 4: Créer la commande update_global_health
────────────────────────────────────────────────

CRÉE le fichier api/management/commands/update_global_health.py
Copie le contenu de: update_global_health.py

Cette commande fait les checks globaux:
  • Comptage tables (reportapp, businesscontinuity, inventory)
  • Status imports (BC, Inventory)
  • Certificat SSL


ÉTAPE 5: Mettre à jour la vue API
──────────────────────────────────

Dans api/views.py, REMPLACE ta classe ClusterStatusView par:
  cluster_view_with_global.py

Cette nouvelle version retourne AUSSI les checks globaux.


ÉTAPE 6: Mettre à jour le dashboard
────────────────────────────────────

REMPLACE templates/cluster_status.html par:
  cluster_status_MODERN.html

Nouveau design:
  ✓ Plus compact et moderne
  ✓ Gradient background
  ✓ Cards plus petites
  ✓ Section globale en bas
  ✓ Animations fluides


ÉTAPE 7: Configurer les CRON
─────────────────────────────

SUR CHAQUE HOST (node1 et node2):

  crontab -e

  # Checks spécifiques au host (toutes les minutes)
  * * * * * cd /opt/app && python manage.py update_node_health
  * * * * * sleep 30; cd /opt/app && python manage.py update_node_health


SUR UN SEUL HOST (node1 par exemple):

  crontab -e

  # Checks globaux (toutes les heures)
  0 * * * * cd /opt/app && python manage.py update_global_health


ÉTAPE 8: Tester
───────────────

  # Test checks host
  python manage.py update_node_health
  
  # Test checks globaux
  python manage.py update_global_health
  
  # Vérifier l'API
  curl http://localhost:8000/api/status | jq
  
  # Ouvrir le dashboard
  http://localhost:8000/status/dashboard


═══════════════════════════════════════════════════════════════════════════════


🎯 RÉSULTAT FINAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DASHBOARD MODERNISÉ :

  ┌────────────────────────────────────────────────────────┐
  │ 🖥️ Cluster Status                    [OK] [Refresh]   │
  └────────────────────────────────────────────────────────┘
  
  ┌────────────────────────────────────────────────────────┐
  │ Nodes: 2  |  Healthy: 2                                │
  └────────────────────────────────────────────────────────┘
  
  ┌─────────────────┐  ┌─────────────────┐
  │ Node 1     [OK] │  │ Node 2     [OK] │  (Compact!)
  │ Hostname: ...   │  │ Hostname: ...   │
  │ ✓ 3 OK         │  │ ✓ 3 OK         │
  └─────────────────┘  └─────────────────┘
  
  ┌────────────────────────────────────────────────────────┐
  │ 📋 Checks Globaux                              [OK]    │
  │ Dernière mise à jour: 15 min                           │
  │ ✓ 7 OK  ⚠ 1 Warning                                   │
  │ [Cliquer pour voir les détails]                        │
  └────────────────────────────────────────────────────────┘


CRON JOBS :

  Node 1:
    • update_node_health → toutes les minutes
    • update_global_health → toutes les heures
  
  Node 2:
    • update_node_health → toutes les minutes


API JSON :

  {
    "cluster_status": "OK",
    "nodes": [...],           ← Checks par host
    "global_checks": {        ← Checks globaux (nouveau!)
      "status": "OK",
      "checks": [...]
    }
  }


═══════════════════════════════════════════════════════════════════════════════


✅ AVANTAGES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ✅ Plus efficace (checks globaux 1x/heure au lieu de 2x/minute)
  ✅ Pas de doublons (tables checkées une seule fois)
  ✅ Dashboard plus moderne et compact
  ✅ Séparation logique (host vs global)
  ✅ Plus facile à maintenir


═══════════════════════════════════════════════════════════════════════════════

                         C'est parti ! 🚀

═══════════════════════════════════════════════════════════════════════════════
