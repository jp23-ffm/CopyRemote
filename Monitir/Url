from django.urls import path
from .views import ClusterStatusView, NodeDetailView

urlpatterns = [
# Vue globale du cluster (lit depuis la DB)
path(‘status’, ClusterStatusView.as_view(), name=‘cluster-status’),

```
# Détails d'un node spécifique
path('status/<str:node_name>', NodeDetailView.as_view(), name='node-detail'),
```

]

# Endpoints disponibles :

# 

# GET /api/status

# -> Statut global du cluster (tous les nodes)

# -> Lit depuis la table NodeHealthStatus

# -> Détecte automatiquement les nodes “stale” (pas de mise à jour récente)

# 

# GET /api/status?details=true

# -> Même chose mais avec le détail de tous les checks de chaque node

# 

# GET /api/status/prod-node-01

# -> Détails complets d’un node spécifique

# -> Tous les checks individuels
