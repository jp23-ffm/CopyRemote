.detail-row {
    display: grid;
    grid-template-columns: minmax(120px, 180px) 1fr;  
    /* Les labels font entre 120px minimum et 180px maximum */
    /* Après 180px, le texte wrap automatiquement */
    gap: 16px;
    padding: 8px 0;
    border-bottom: 1px solid #e5e7eb;
    font-size: 13px;
}

.detail-label {
    font-weight: 600;
    color: #4b5563;
    overflow: hidden;
    text-overflow: ellipsis;
    /* Retirer le white-space: nowrap pour permettre le wrap si > 180px */
}
