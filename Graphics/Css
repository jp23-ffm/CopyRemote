/* Dans charts.html <style> */
.export-table-btn {
    background: var(--btn-secondary);
    color: white;
    border: none;
    padding: 8px 16px;
    border-radius: 4px;
    cursor: pointer;
    font-size: 13px;
    margin: 15px 0;
    transition: all 0.2s;
    display: inline-block;
}

.export-table-btn:hover {
    background: var(--btn-secondary-hover);
    transform: translateY(-1px);
    box-shadow: 0 2px 4px var(--shadow);
}
